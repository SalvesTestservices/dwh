<% if @title.blank? %>
  <th scope="col" class="align-top relative px-2 py-3"></th>
<% else %>
  <th scope="col" class="align-top px-2 py-3 <%= @align %> <%= @mobile %> text-xs font-medium text-white uppercase truncate tracking-wider">
    <%= @title %>
  </th>
<% end %>