<% if @title.blank? %>
  <div class="table-cell align-top relative px-2 py-3"></div>
<% else %>
  <div class="table-cell align-top px-2 py-3 <%= @align %> text-sm font-bold text-gray-700 uppercase tracking-wider">
    <%= @title %>
  </div>
<% end %>