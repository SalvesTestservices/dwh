<div class="w-full pt-5 pb-16 flex flex-row justify-end">
  <div class="w-1/2">
    <%= link_to image_tag("logo.svg", alt: "Logo"), root_path %>
  </div>
  <div class="w-1/2 flex flex-row justify-end pt-2">
    <% if current_user %>
      <%= link_to I18n.t(".devise.sessions.signout"), destroy_user_session_path, data: { turbo_method: :delete }, class: "text-gray-800 hover:underline pr-5" %>
    <% end %>
  </div>
</div>