<div class="flex h-screen bg-gray-50">
  <!-- Sidebar -->
  <div class="w-64 bg-white border-r border-gray-200 shadow-sm">
    <div class="p-4">
      <h2 class="text-lg font-semibold text-gray-700 mb-4">Navigation</h2>
      <nav class="space-y-1">
        <%= link_to datalab_reports_path, class: "flex items-center px-4 py-3 text-gray-700 rounded-lg hover:bg-gray-100 transition-colors duration-150" do %>
          <%= render(View::IconComponent.new(icon_type: "datalab", size: "small", color: "gray")) %>
          <span class="ml-3"><%= I18n.t(".site.titles.data_lab") %></span>
        <% end %>

        <%= link_to dwh_dp_pipelines_path, class: "flex items-center px-4 py-3 text-gray-700 rounded-lg hover:bg-gray-100 transition-colors duration-150" do %>
          <%= render(View::IconComponent.new(icon_type: "pipeline", size: "small", color: "gray")) %>
          <span class="ml-3"><%= I18n.t(".dp_pipeline.titles.index") %></span>
        <% end %>

        <%= link_to data_targets_path, class: "flex items-center px-4 py-3 text-gray-700 rounded-lg hover:bg-gray-100 transition-colors duration-150" do %>
          <%= render(View::IconComponent.new(icon_type: "target", size: "small", color: "gray")) %>
          <span class="ml-3"><%= I18n.t(".data_target.titles.index") %></span>
        <% end %>

        <%= link_to data_governance_index_path, class: "flex items-center px-4 py-3 text-gray-700 rounded-lg hover:bg-gray-100 transition-colors duration-150" do %>
          <%= render(View::IconComponent.new(icon_type: "certificate", size: "small", color: "gray")) %>
          <span class="ml-3"><%= I18n.t(".data_governance.titles.index") %></span>
        <% end %>

        <%= link_to api_docs_path, class: "flex items-center px-4 py-3 text-gray-700 rounded-lg hover:bg-gray-100 transition-colors duration-150" do %>
          <%= render(View::IconComponent.new(icon_type: "api", size: "small", color: "gray")) %>
          <span class="ml-3"><%= I18n.t(".site.titles.data_api") %></span>
        <% end %>

        <%= link_to users_path, class: "flex items-center px-4 py-3 text-gray-700 rounded-lg hover:bg-gray-100 transition-colors duration-150" do %>
          <%= render(View::IconComponent.new(icon_type: "users", size: "small", color: "gray")) %>
          <span class="ml-3"><%= I18n.t(".user.titles.index") %></span>
        <% end %>

        <%= link_to roles_path, class: "flex items-center px-4 py-3 text-gray-700 rounded-lg hover:bg-gray-100 transition-colors duration-150" do %>
          <%= render(View::IconComponent.new(icon_type: "lock", size: "small", color: "gray")) %>
          <span class="ml-3"><%= I18n.t(".role.titles.index") %></span>
        <% end %>
      </nav>
    </div>
  </div>

  <!-- Main Content Area -->
  <div class="flex-1 overflow-auto">
    <div class="p-8">
      <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
        <h1 class="text-2xl font-semibold text-gray-800 mb-6">Dashboard</h1>
        <!-- Dashboard content will go here -->
        <div class="text-gray-500">
          Dashboard content to be added later
        </div>
      </div>
    </div>
  </div>
</div>