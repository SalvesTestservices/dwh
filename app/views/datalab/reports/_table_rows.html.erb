<% report_data[:rows].each do |row| %>
  <tr>
    <% report_data[:columns].each do |column| %>
      <td class="p-1 text-left text-sm text-gray-900 border border-gray-400 bg-white">
        <%= row[column[:id]] %>
      </td>
    <% end %>
  </tr>
<% end %>
